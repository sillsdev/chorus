#!/bin/sh

echo "Running ChorusMerge.exe with dotnet..."

if [ -e `dirname $0`/ChorusMerge.dll ]
then
	exec dotnet `dirname $0`/ChorusMerge.dll "$@"
else
	exec dotnet run `dirname $0`/ChorusMerge.exe "$@"
fi
